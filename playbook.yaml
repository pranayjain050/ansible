---
- name: Create Nginx Server
  hosts: webserver
  tasks: 
  - name: add epel release
    yum:
      name: epel-release
      state: present
  - name: install nginx server
    yum: 
      name: nginx
      state: present
  - name: start nginx service
    service:
      name: nginx
      state: started
